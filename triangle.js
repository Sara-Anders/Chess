"use strict";
//start by creating for() to loop results and make triangle with #
for (let digit = "#";
//get length of variable, needs to read length, ends reading length less than 8 stopping loop
digit.length < 8;
//digit = digit + # add # to end of each line up to 8
digit += "#"
)
//show results of looping
console.log(digit);

